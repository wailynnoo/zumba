[build]
# Use Dockerfile.member-api for building (relative to fitness-dance-backend root)
dockerfilePath = "../Dockerfile.member-api"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

